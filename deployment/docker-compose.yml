name: price-tracker-bot

services:
  price_tracker_bot:
    build: ..
    image: price_tracker_bot
    container_name: price_tracker_bot
    env_file: ../.env
    restart: unless-stopped
    # Only needed while the webhook mode exists; remove once the bot is long polling only
    ports:
      - "7080:8080"
